/* Public endpoint only. The published API URL is configured at deployment. */
window.MOON_RANKING_CONFIG = window.MOON_RANKING_CONFIG || { apiBase: 'https://moon-games-ranking.abccasfda.chatgpt.site' };
